d /var/lib/universal-lite 0755 root root -
f /var/lib/universal-lite/greeter-theme 0666 root root - light
# Xwayland socket directory — must be world-writable (sticky) so the
# user session can create sockets after cage (greeter) exits.
d /tmp/.X11-unix 1777 root root -