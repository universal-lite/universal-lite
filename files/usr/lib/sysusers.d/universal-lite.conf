# Ensure the video group exists for brightnessctl backlight access.
# On bootc systems, /etc/group from the build may not persist;
# sysusers.d is processed by systemd-sysusers on every boot.
g video 39
