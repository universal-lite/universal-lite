[Unit]
OnFailure=universal-lite-chrome-oom-cleanup.service

[Scope]
# Chrome creates delegated child cgroups for renderers. Treat the Flatpak app
# scope as one workload so OOMD/kernel OOM cannot leave a half-dead browser UI.
OOMPolicy=kill
